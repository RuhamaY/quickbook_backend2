/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // HTTPS will be handled via --experimental-https flag or reverse proxy
  // For production, use a reverse proxy (nginx, Caddy) or hosting platform with HTTPS
};

module.exports = nextConfig;


